{
  "compilerOptions": {
    "composite": true,                       // Enable composite project
    "declaration": true,                     // Emit declaration files (.d.ts)
    "emitDeclarationOnly": false             // Ensure both JS and d.ts files are emitted
  },
  "include": ["src/**/*.ts"]                 // Include all .ts files in src folder
}
